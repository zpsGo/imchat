<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zps.imchat.mapper.UserMapper">

    <select id="login" resultType="com.zps.imchat.bean.User">
        select * from im_user where id=#{id} and pass=#{pass}
    </select>

</mapper>
